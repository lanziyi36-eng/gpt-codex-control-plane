# Reviews

这个目录保存跨任务 review notes、review summaries 或 human-requested review artifacts。

单个任务的正式 Brain review 应优先写入：

```text
tasks/<TASK_ID>/review.md
```

Brain review 必须给出 `MERGE`、`REQUEST_CHANGES`、`REJECT` 或 `BLOCKED_NEEDS_HUMAN_DECISION` 中的一种建议。
