# Human Approvals

这个目录保存 human approval records。任何扩大权限、访问 secrets、启用 network、合并 PR、跳过检查或修改 isolation 设置的决定，都应在这里留下记录。

建议文件命名：

```text
YYYY-MM-DD-TASK_ID-approval.md
```

每条记录应包含审批人、时间、审批范围、相关 PR 或 task，以及是否有过期时间。
