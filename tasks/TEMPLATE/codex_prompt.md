# T-XXXX-short-name Codex Prompt

你是 Codex Executor。请先阅读 `AGENTS.md`、`SWITCHBOARD.md`、`status/PROJECT_STATUS.md` 和本任务文件夹。

执行目标见 `brief.md`。验收标准见 `acceptance.md`。

约束：

- 只编辑 `brief.md` 中列出的 allowed files。
- 不放宽 safety switches。
- 不提交 secrets、sessions、tokens、auth artifacts 或本地私密配置。
- 完成后更新 `result.md`，并将 `review.md` 留给 ChatGPT Brain 或 human。
